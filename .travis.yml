sudo: required
language: generic

env:
  - TRAVIS_DEBIAN_DISTRIBUTION=ubuntu/xenial

services:
  - docker

script:
  - wget -O- http://travis.debian.net/script.sh | sh -

branches:
  except:
    - /^debian\/\d/
    - /^upstream.*/
